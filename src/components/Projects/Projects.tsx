import ProjectsDepth from './ProjectsDepth';

const Projects = () => {
    return (
        <section id="projects" className="relative w-full min-h-screen">
            <ProjectsDepth />
        </section>
    );
};

export default Projects;
